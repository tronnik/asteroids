#include <iostream>
#include "raylib.h"
#include "player.h"

using namespace std;

int main()
{
    // Inicialización
    InitWindow(1024, 768, "Mi juego");

    

    // Loop
    while (!WindowShouldClose())
    {
        // Chequeo de Input

        


        // Actualización
        

        // Dibujado
        BeginDrawing();
        ClearBackground(WHITE); // system("cls");
       
        

        EndDrawing();
    }

    // Cierre
    CloseWindow();
}